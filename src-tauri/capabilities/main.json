{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The webview listens for the usage snapshots the tray refresh publishes. Everything else it calls is a command of this app, which needs no permission.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
