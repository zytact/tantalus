{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The webview receives usage snapshots pushed by the tray refresh. Everything else it needs is a command of this app, which needs no permission.",
  "windows": ["main"],
  "permissions": ["core:event:default"]
}
